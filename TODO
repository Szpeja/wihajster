TOP LEVEL:

+ Stop fucking around.
+ Connect to printer. DONE.
- Emmit commands to printer. ALMOST. Fix concurency.
- Allow emmiting commands by console. STARTED.
+ Allow loading scripts. DONE
- Generate documentation for methods. ALMOST. Add generation of codes to yard.
+ Allow to steer with gamepad.
- Emit proper commands on gamepad moves.
- Add proper tests.
- Add opengl visualization of scripts. (simple version).

ALSO:

+ Fix instantization of app and lib. Make sure there's ONE master object. there should be no possiblity of instantizing two event loops or ui's
+ Auto reloading of scripts using guard lib.
