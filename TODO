TOP LEVEL:

+ Stop fucking around.
+ Connect to printer.
+ Emmit commands to printer. NEEDS TESTING
+ Allow emmiting commands by console. ALMOST.
+ Allow loading scripts.
+ Generate documentation for methods.
+ Allow to steer with gamepad.
- Emit proper commands on gamepad moves. CHECK: units, relative vs. absolute. Feed rates
- Add proper tests.
- Add opengl visualization of scripts. (simple version).

ALSO:

+ Fix instantization of app and lib. Make sure there's ONE master object. there should be no possiblity of instantizing two event loops or ui's
+ Auto reloading of scripts using guard lib.
